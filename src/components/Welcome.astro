---
import { secureFetch } from "../lib/secureFetch";

const playlists = await secureFetch("playlists");
---

<script type="module">
  import { animate } from "motion";
  animate(".box", { rotate: 360, opacity: [0, 1] }, { duration: 2 });
</script>
